{
  "scripts": {
    "hre:compile": "hardhat compile",
    "hre:deploy": "hardhat run scripts/deploy.ts --network $npm_config_host",
    "hre:node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.18"
  }
}
